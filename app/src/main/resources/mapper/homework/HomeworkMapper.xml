<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="loaa_backend.homework.mapper.HomeworkMapper">
    <select id="getHomeworkList" resultType="HomeworkModel">
        SELECT * FROM homework;
    </select>

    <select id="getCharacterList" resultType="CharacterModel">
        SELECT * FROM loaa.`character`;
    </select>
</mapper>